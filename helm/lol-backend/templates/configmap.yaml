apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lol-backend.fullname" . }}-config
  labels:
    {{- include "lol-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
