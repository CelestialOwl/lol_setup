apiVersion: v1
kind: Service
metadata:
  name: {{ include "lol-backend.fullname" . }}
  labels:
    {{- include "lol-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "lol-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
