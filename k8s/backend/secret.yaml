apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: lol-tracker
  labels:
    app: lol-backend
type: Opaque
# Values are base64-encoded. Replace with your actual secrets:
#   echo -n 'your-value' | base64
stringData:
  DB_PASSWORD: "postgres123"   # replace before production
  RIOT_API_KEY: "RGAPI-REPLACE-ME"
